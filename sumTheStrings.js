/**
 * This function takes 2 integers in form of a string as an input, and outputs the sum (also as a string)
 * @param {string} a 
 * @param {string} b 
 * @returns The sum of the strings as a string
 */
function sumStr(a,b) {
    return Number(a) + Number(b) + ""
  }